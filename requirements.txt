# vidrag — pinned to minor versions; upgrade deliberately, not implicitly
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
httpx>=0.27

# ingest
yt-dlp>=2026.6.9
gallery-dl>=1.27          # Instagram Reels (Phase 4-5; primary per rule 2)
youtube-transcript-api>=1.0
groq>=0.13
trafilatura>=1.12         # web article extraction (Phase 4 article adapter)

# transcript post-processing (Simplified -> Taiwan Traditional)
opencc>=1.1

# rag
fastembed>=0.4
sqlite-vec>=0.1.6
anthropic>=0.40

# admin page (QR for the iOS shortcut installer)
segno>=1.6
